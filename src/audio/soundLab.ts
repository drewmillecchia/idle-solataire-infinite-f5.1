/**
 * A tiny auditioning list for the Settings panel's "test sounds" row: every name the synth
 * presenter (`presenters.ts`) handles, in one place so the UI never hardcodes its own copy.
 */
export { SOUND_NAMES } from './presenters';
