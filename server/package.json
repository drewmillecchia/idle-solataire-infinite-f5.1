{
  "name": "isi-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloud-save API for Idle Solitaire Infinite (Stage 1: Hono + node:sqlite).",
  "scripts": {
    "dev": "tsx watch src/node.ts",
    "start": "tsx src/node.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.7"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "vitest": "^5.0.0",
    "typescript": "^5.9.3",
    "@types/node": "^24.0.0"
  },
  "engines": { "node": ">=22.5.0" }
}
